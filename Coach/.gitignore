# --- logs ---
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# --- node / vite ---
node_modules
dist
dist-ssr
*.local
# vite caches (usually inside node_modules, but just in case)
.vite/
*.tsbuildinfo

# --- editors / os ---
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# --- python backend ---
# virtual envs
.venv/
server/.venv/
# bytecode & caches (recursive)
**/__pycache__/
**/*.py[cod]
# test & type caches
.pytest_cache/
.mypy_cache/
.pyright/
.coverage
htmlcov/

# --- local data & secrets ---
# sqlite / data
server/app/data/
*.sqlite3
*.db

# dotenv (all variants)
.env
.env.*
server/.env
server/.env.*

